language: generic
sudo: required

addons:
    apt:
        packages:
            - texlive-full
            - latexmk
            - asymptote
script:
    - make

deploy:
    provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
